/*const express = require("express");
const router = express.Router();

const verificarAuth = require("../middleware/authMiddleware");
const criancaController = require("../Controller/criancaController");

router.post("/", verificarAuth, criancaController.create);

router.put("/:id", verificarAuth, criancaController.update);

router.delete("/:id", verificarAuth, criancaController.remove);

router.get("/:id", verificarAuth, criancaController.findById);

module.exports = router;*/